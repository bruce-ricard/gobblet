server = {
  log_level = DEBUG
}

postgres = {
  (* Postgres host *)
  host = "localhost"
  (* Postgres port *)
  port = ""
  (* Postgres database *)
  database = "gobblet"
  (* Postgres user *)
  user = "gobblet_server_user"
  (* Postgres password *)
  password = ""
}
